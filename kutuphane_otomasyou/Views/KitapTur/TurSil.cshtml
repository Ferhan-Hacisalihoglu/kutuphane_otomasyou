@model List<kutuphane_otomasyou.Models.table.kitaplar.KitapTuru>
@using kutuphane_otomasyou.Models.table.kitaplar

@{
    ViewBag.Title = "TurSil";
}


<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>


<style>

    #sefiller {
        background-color: rgba(255, 255, 255, 0.9); /* Beyaz rengin %50 opaklıkta görünmesini sağlar */
        background-blend-mode: lighten;
        background-size: cover;
        background-position: center;
    }
</style>

@*@if (Model != null)
    {*@
<body>



    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Kitap Turu Adı</th>
                <th scope="col">Sil veya Düzenle</th>
            </tr>
        </thead>
        <tbody>
            @foreach (KitapTuru tur in Model)
            {
                <tr>
                    <td>@tur.tur_adi</td>
                    <td>
                        <form method="post" asp-controller="Kitap" asp-action="Sil">
                            <input type="hidden" name="kitapId" value="@tur.Id" />
                            @Html.ActionLink("Sil", "TurSil", "KitapTur", new { TurId = tur.Id }, new { @class = "btn btn-danger" })
                            @Html.ActionLink("Düzenle", "TurDuzenle", "KitapTur", new { TurId = tur.Id }, new { @class = "btn btn-warning", @style = "color: white;" })


                        </form>
                    </td>
                </tr>
            }

            @if (ViewBag.hata != null)
            {
                <p style="color: red;text-align:center">@ViewBag.hata </p>
            }
        </tbody>
    </table>

    @*@{
            bool gizliVarMi = Session["gizli"] as bool? ?? false;
            if (gizliVarMi)
            {
                <div style="text-align: center;">
                    <button type="button" class="btn btn-info mb-3" onclick=""> YENİ KİTAP EKLE</button>
                </div>
            }
        }*@



</body>
@*}
    else
    {

        < p > Model boş.</ p >
    }*@
</html>

